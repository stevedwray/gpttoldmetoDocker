# Default variables for registry
registry:
  ip: "{{ registry_ip }}"
  subject_alt_name: registry.lan.local
  volumes:
    data: /srv/usb/registry/data
    certs: /srv/usb/registry/certs
    auth: /srv/usb/registry/auth
  c: NZ
  st: Wellington
  l: Wellington
  o: Stephen Wray
  ou: ChatGPT Docker
