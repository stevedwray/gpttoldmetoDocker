network:
  version: 2
  renderer: networkd
  ethernets:
    {{ physical_nic }}:
      addresses: [{{ pi4a_addr }}]
      gateway4: {{ network_gateway }}
      routes:
        - to: 0.0.0.0/0
          via: {{ network_gateway }}
      nameservers:
        addresses: [{{ dns_server_address }}]
